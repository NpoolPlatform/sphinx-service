package api

import (
	"context"

	"github.com/NpoolPlatform/message/npool/trading"
)

func (s *Server) GetTransactions(ctx context.Context, in *trading.GetTransactionsRequest) (resp *trading.GetTransactionsResponse, err error) {
	return
}
