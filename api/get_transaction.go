package api

import (
	"context"

	"github.com/NpoolPlatform/message/npool/trading"
)

func (s *Server) GetTransaction(ctx context.Context, in *trading.GetTransactionRequest) (resp *trading.GetTransactionResponse, err error) {
	return
}
